{
  "name": "andrea-bbq-menu",
  "private": true,
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local scripts/dev-server.cjs",
    "test": "node --test scripts/admin.test.cjs",
    "check:menu": "node scripts/check-menu.cjs",
    "admin:secrets": "node scripts/admin-secrets.cjs"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
